fastapi
hypercorn
httpx
sqlalchemy
openai
alembic
python-dotenv
psycopg2-binary # For PostgreSQL, as DATABASE_URL indicates future Postgres use

# For RAG with FAISS (local, as per user notes)
faiss-cpu # Or faiss-gpu if GPU is available and intended

# Consider adding for development/linting if not managed elsewhere
# ruff
# pytest

